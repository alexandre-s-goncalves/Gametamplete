import { colors } from '../colors';

export const darkTheme = {
  mode: 'dark',
  colors: {
    background: colors.background06,
    card: colors.background05,
    text: colors.white,
    primary: colors.vividIndigo,
    border: colors.charcoal,
  },
};
