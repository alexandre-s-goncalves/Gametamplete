import { colors } from '../colors';

export const lightTheme = {
  mode: 'light',
  colors: {
    background: colors.background01,
    card: colors.background02,
    text: colors.darkContent,
    primary: colors.vividIndigo,
    border: colors.neutralLight,
  },
};
